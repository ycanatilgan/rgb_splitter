# rgb-splitter

Split RGGB CFA FITS files into R/G/B channel FITS files using superpixel extraction (no interpolation).

## Install (local)
pip install .

## Usage
rgb-splitter --input ./data --output ./output_rgb
